import { CodeCompare } from '@/constant/codeBlock';

export const SkeletonSix = () => {
  return (
    <div className="p-8 overflow-visible h-full relative">
      <CodeCompare className="relative z-10" />
    </div>
  );
};